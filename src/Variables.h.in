/*  Used to set CERTIFICATES_PATH variable from CMakeList.txt file
    If you want to change the path of the executable, then you need to specify this on your own and rebuild only
    with `make` command
*/
#define CERTIFICATES_PATH "${CERTIFICATES_PATH}"